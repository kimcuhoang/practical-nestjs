import { SignatureType, Signature, ESignature, PaymentSignature } from "./models/signatures";
import { Surcharge, SurchargeArguments, SurchargeType } from "./models/surcharge.entities";

export const DomainModels = {
    SignatureType,
    Signature,
    ESignature,
    PaymentSignature,
    SurchargeType,
    Surcharge,
    SurchargeArguments
};